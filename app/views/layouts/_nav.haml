#nav
  #logo
    = link_to image_tag("/img/matchupslogo.png"), "/"
  #user
    - if user_signed_in?
      #facebookLoggedIn
        = image_tag("../img/facebook-button-logo.png")
        .name= current_user.name
      #signOut= link_to " Sign Out", "/users/sign_out"
    - else
      #facebookLoggedIn
        = image_tag("../img/facebook-button-logo.png")
        .name= link_to "Login with Facebook", user_omniauth_authorize_path(:facebook)
