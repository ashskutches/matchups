-if current_user && current_user.admin
  #adminInterface
    %h2 Admin
    %ul
      %li= link_to "Users", users_path
      %li= link_to "Posts", matchups_path
      %li= link_to "Feedback", feedbacks_path
