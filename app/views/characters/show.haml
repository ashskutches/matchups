%h1
  =image_tag("/img/characters/#{character.name}.jpg")
= image_tag("/img/names/#{character.name}.png", :height => "10%")
%h2 Select a character!
#chooseYourCharacter
  %ul
  - characters.each do |char|
    %li.linkWithTips= link_to image_tag("/img/characters/#{char.name}.jpg"), character_path(char)
#section2
  %h2 Tips
  %hr
  - unless characterTips.empty?
    %ul.tips
      - characterTips.each do |tip|
        %li
          %h3
            = link_to tip.player, character_path(Character.find_by_name(tip.player))
            = link_to image_tag("../img/characters/#{tip.player}.jpg"), character_path(Character.find_by_name(tip.player))
            VS
            = link_to image_tag("../img/characters/#{tip.opponent}.jpg"), character_path(Character.find_by_name(tip.opponent))
            = link_to tip.opponent, character_path(Character.find_by_name(tip.opponent))
          %p=link_to truncate(tip.body, :length => 150, :omission => "....."), tip_path(tip)
          .userName= tip.user.name
          = ':'
          = time_ago_in_words(tip.created_at)
          ago
  - else
    %br
    %h3 Select an opponent and be the first to post a tip!
