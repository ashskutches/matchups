#posts
  %table
    %th
      %h2
        = @character.name
    - @matches.sort.each_slice(4) do |group|
      %tr
        -group.each do |match|
          %td
            vs
            = link_to "#{match.opponent.name}", matchup_path(match)
